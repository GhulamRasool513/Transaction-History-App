import 'package:flutter/material.dart';

const Color scaffoldBackgroundColor = Color(0xff418DE5);

const whiteContainerDecoration = BoxDecoration(
    color: Colors.white,
    borderRadius: BorderRadius.only(
        topRight: Radius.circular(40.0),
        topLeft: Radius.circular(40.0)));

const dividerLine = Divider(thickness: 1,);

